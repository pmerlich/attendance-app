# ביקורת ותיקונים מאוחדת — עובד עצמאי

עודכן: 2026-09-03. מסמך זה מאחד את הביקורת המקורית עם הממצאים שאומתו מתוך `PROJECT_AUDIT_HE.md` מול הקוד הנוכחי. לא בוצעו עדיין תיקוני קוד.

מקרא: `[ ]` ממתין · `[~]` בטיפול · `[x]` טופל ונבדק · `[?]` דרושה החלטה · `מותנה` דורש אימות בסביבת הפריסה.

## סדר העבודה המומלץ

1. **P0 — הגנה על זהות ובידוד מידע מקומי.** לפני שימוש בנתונים אמיתיים.
2. **P1 — שלמות נתונים וסנכרון.** אטומיות, התנגשויות, שגיאות תור ומזהים יציבים.
3. **P1 — כסף וזמן.** דיוק כספי, תאריכים, טיימר וסיכומים מלאים.
4. **P2 — מחזור חיים, ביצועים ופריסה.** סל מחזור, קבצים, migrations, אינדקסים ו־Service Worker.
5. **P3 — חוויית שימוש ונגישות.** ניווט, חיפוש, ניסוח, מטרות מגע ותצוגות.
6. **בדיקות רוחב.** להוסיף לכל שלב בדיקות אינטגרציה ותרחיש לפני שמסמנים אותו כטופל.

## P0 — אבטחה ובידוד

- [~] **S-24 — אמון בכותרות זהות (אומת בקוד; חומרה מותנית בפריסה):** הדומיין הציבורי מתעלם כעת מכותרות זהות שמגיעות מהלקוח ומצב האורח לקריאה בלבד. נותר לאמת בחוזה הפריסה שה־proxy בדומיין המאומת מוחק ומזריק מחדש את כותרות `oai-authenticated-*`.
- [x] **S-25 — בידוד IndexedDB לפי זהות:** מסד ומפתחות נפרדים לפי `businessId:userId`; בחיבור מקוון הזהות נקבעת מהשרת לפני קריאת התור וסנכרונו.
- [x] **S-26 — מצב אורח ציבורי:** הדגמת האורח המשותפת היא לקריאה בלבד, כולל חסימת JSON והעלאות.
- [x] **S-27 — CSP:** `unsafe-inline` הוסר מ־`script-src`; build ובדיקת header נוספו. inline style נשאר זמנית לצורכי framework.

## P1 — שלמות נתונים וסנכרון

- [x] **S-28 — נתונים ו־audit אטומיים:** פעולות הכתיבה, שיוכי הפרויקט וה־audit אוגדו ב־`db.batch()`; עובדים נבדקים לפני תחילת עדכון פרויקט.
- [~] **S-29 — בקרת התנגשויות:** עדכון פרויקט ומצבו שולחים `expectedUpdatedAt`; השרת מחזיר `409` מובנה והשינוי נשמר בתור עם הסבר. נותר להרחיב את אותו חוזה לכל הישויות הניתנות לעריכה.
- [x] **S-05 — שגיאות סנכרון ברורות:** דחייה נשמרת על הפעולה עם סיבת השרת, פעולות אחרות ממשיכות, והמשתמש יכול לנסות שוב או להסיר פעולות שנדחו.
- [x] **S-08 — לקוח לפי מזהה:** פרויקטים מחזירים ושולחים `clientId`; יצירה, עריכה, מחיקה ושחזור אופטימיים משייכים לפי מזהה גם כשיש שמות כפולים.
- [x] **S-09 — יצירת לקוח ופרויקט אטומית:** הטופס שולח פעולה אחת, והלקוח, הפרויקט, השיוכים וה־audit נכתבים יחד.
- [ ] **S-14 — ריבוי לשוניות:** לעדכן timer/state בין לשוניות בעזרת `BroadcastChannel` או אירועי storage, ולבדוק התנגשויות.

## P1 — כסף, זמן ודוחות

- [~] **S-01 — עדכון מקומי של הסיכומים:** זמן, טיימר, תשלום והוצאה יעדכנו מיד שעות, הכנסה, הוצאות, רווח ויתרה, גם באופליין.
- [ ] **S-30 — כסף כמספר עשרוני ומטבע קשיח (אומת):** סכומים נשמרים כ־`real`, מחושבים ב־JavaScript, ובממשק/יצירת פרויקט EUR ו־`€` קשיחים למרות `businesses.currency`. יש לשמור יחידות מטבע קטנות כמספר שלם, להעביר currency ב־state ולהשתמש ב־`Intl.NumberFormat`.
- [x] **S-03 — ולידציית זמן ותאריך אחידה:** השרת מאמת תאריך קלנדרי ודוחה תאריכים עתידיים או שגויים ב־`400`, ללא החלפה שקטה.
- [x] **S-31 — עצירת טיימר לפי מזהה:** הלקוח שולח `timerId`; השרת סוגר ומבקר רשומה אחת בלבד ומחזיר `409` אם אינה פעילה או אינה שייכת למשתמש.
- [x] **S-04 — רשימות וסכומים מלאים:** מגבלות 50/100/1000 הוסרו; ה־state, פעילות הפרויקט והייצוא משתמשים בנתונים מלאים. נוספו אינדקסים למסלולים החמים.
- [?] **S-06 — הכנסה גלובלית בתקופה:** לקבוע שיוך לפי פתיחה, סיום או תשלום.
- [ ] **S-07 — אגורות ותצוגה:** להציג שתי ספרות בכל סכום, יתרה, דוח וייצוא, ללא `Math.round`.
- [ ] **S-02 — שיוך טיימר יציב:** פתיחת פרויקט אחר לא תשנה את הפרויקט שמוצג בטיימר הפעיל.
- [ ] **S-17 — זמן אחיד:** דוחות, הדפסה וייצוא יציגו `HH:MM:SS`.

## P2 — מחזור חיים, ביצועים ופריסה

- [ ] **S-32 — מדיניות מחיקה ושחזור מלאה (אומת חלקית):** הטענה המקורית ששחזור אינו מחזיר `project_workers` אינה מדויקת—השייכויות אינן נמחקות ולכן חוזרות עם הפרויקט. עם זאת, שחזור עובד מכריח `is_active=1` ואינו משחזר בהכרח את מצבו הקודם. יש להגדיר snapshot/soft-delete עקבי ולבדוק לקוח+פרויקטים, פרויקט+עובדים ועובד משויך.
- [ ] **S-33 — מחיקה סופית וניקוי R2 (אומת לאחר צמצום):** מחיקת פרויקט/לקוח אינה מוחקת attachment או object מ־R2; זה תקין עבור שחזור, אך אין purge סופי או job לניקוי יתומים. יש להגדיר retention, מחיקה סופית וניקוי מפצה. שחזור רגיל אמור להשאיר את הקבצים זמינים.
- [ ] **S-22 — קבלה של הוצאה מחוקה:** להגדיר אם הקבלה נשמרת עם ההוצאה, חוזרת בשחזור ומתי נמחקת סופית.
- [~] **S-34 — schema setup:** נוספה migration ייעודית והכנת schema רצה פעם אחת בלבד לכל isolate במקום בכל בקשה. נותר להסיר את fallback לאחר אימות תהליך הפריסה.
- [x] **S-35 — אינדקסים למסלולים חמים:** נוספו אינדקסים מורכבים לפרויקטים, לקוחות, audit ודיווחי זמן, גם בסכימה וגם ב־migration.
- [ ] **S-36 — versioning ל־Service Worker (אומת):** cache ידני `v3` ו־runtime caching של HTML/assets עלולים ליצור shell מעורבב אחרי deployment. להשתמש ב־build hash/precache manifest ולבדוק עדכון שנכשל באמצע.
- [ ] **S-37 — validation מרכזי ל־payloads (אומת חלקית):** יש מגבלת body והרבה בדיקות שדה טובות, לכן הטענה שאין הגבלות אינה נכונה במלואה. עדיין קיימים coercion/fallback שקטים ו־`workers` נחתך ל־100 במקום להידחות. לאחד schemas ולהחזיר שגיאות עקביות.

## P3 — מוצר, UX ונגישות

- [ ] **S-10 — פרטי עבודה:** תיאור/הערות, איש קשר וטלפון, תאריך התחלה, יעד ותאריך סיום.
- [ ] **S-11 — מצב כספי:** טרם חויב, חויב, שולם חלקית או שולם במלואו.
- [x] **S-12 — היסטוריית מסכים:** המסך והפרויקט נשמרים ב־URL; Back/Forward, רענון וקישור ישיר משחזרים אותם.
- [ ] **S-13 — זמן במובייל:** מסכה או שדות נוחים לשעות/דקות/שניות במקום הקלדת נקודתיים בעייתית.
- [ ] **S-15 — קבלות באופליין:** שמירה מקומית והעלאה ברקע עם מצב ברור.
- [ ] **S-16 — ניסוח אישי:** להסיר במצב עצמאי ניסוחי צוות ועובדים.
- [ ] **S-18 — מטרות מגע:** לפחות 44×44 במובייל.
- [ ] **S-19 — כרטיס נגיש:** להסיר `role=button` שעוטף פקדים או ליישם מבנה סמנטי תקין.
- [ ] **S-20 — יומן שימושי:** להציג מה השתנה בפועל ולא רק פעולה כללית.
- [ ] **S-21 — איתור רשומות:** חיפוש, טווחי תאריכים ומיון ברשימות.

## בדיקות חובה לפני מסירה

- [ ] **S-23 — בדיקות תרחיש:** הרשאות manager/employee; בידוד שני עסקים; זיוף כותרות; הזמנה/תפוגה/reuse; offline עם retry, `401`, `409` ו־`5xx`; שתי לשוניות/מכשירים; rollback של DB/audit/R2; שחזור מלא; MIME וחתימת קובץ; תאריכים לא חוקיים/עתידיים; דיוק כספי; pagination; ניווט ומובייל.

## ממצאים שלא הועתקו כלשונם

- **“סל המחזור אינו משחזר שיוכי עובדים”** — לא הוכח: `project_workers` אינם נמחקים במחיקת פרויקט או עובד, ולכן השיוך נשמר. נשארה משימת בדיקה ושיפור עקביות תחת S-32.
- **“מחיקת פרויקט משאירה קבצים ולכן השחזור שבור”** — הניסוח שגוי: שמירת הקבצים נדרשת לשחזור. הפער הוא היעדר purge/retention וניקוי יתומים, תחת S-33.
- **“אין מגבלות payload מספקות”** — נכון חלקית בלבד: קיימות מגבלות body, טקסט, סכומים, מזהים, MIME וחתימות קובץ. נותר לאחד validation ולמנוע תיקון שקט, תחת S-37.

## יומן

- 2026-09-03 — הביקורת המקורית תועדה; S-01 עבר לטיפול.
- 2026-09-03 — `PROJECT_AUDIT_HE.md` אומת מול הקוד וממצאיו שולבו לפי עדיפות. העבודה נעשתה בענף `audit/consolidate-findings`; `main` לא שונה.
