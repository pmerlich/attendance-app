# סטטוס עבודה

עודכן לאחרונה: 2026-08-26

## מצב נוכחי

- שלב פעיל: שלב 2 — ליבה מתמשכת.
- סטטוס: בתהליך.
- ענף עבודה: `feature/employee-invitations`.

## הושלם

- נאספו ונותחו תשובות שאלון האפיון.
- הוגדרו מטרת המוצר, המשתמשים וגבולות ה־MVP.
- הוחלט להתחיל עם כמה עובדים, EUR ומודל חיוב משולב.
- נוצרו תוכנית מוצר, יומן החלטות וקובץ סטטוס.
- נוסף כלל קבוע המחייב קריאת המסמכים ועדכונם.
- מסמכי היסוד נשמרו ב־Git ב־commit `64bc786`.
- נבחרה תשתית React/TypeScript עם Vinext ופריסה תואמת Cloudflare.
- הוגדרה סכמת נתונים ראשונית ללקוחות, פרויקטים, עובדים, שעות, תשלומים, הוצאות וקבצים.
- נוצרה מיגרציית מסד נתונים ראשונה.
- נבנה מסך ראשי RTL רספונסיבי למחשב ולטלפון.
- הטיימר מאפשר הפעלה, השהיה, סיום ומעבר בין פרויקטים.
- ניתן לסנן את רשימת הפרויקטים לפי מצב.
- בניית היישום הושלמה בהצלחה.
- אב־הטיפוס נשמר ב־commit `88c848a` והועלה לענף `feature/mvp-dashboard` ב־GitHub.
- שם המוצר שונה ל"מנהל עבודה".
- נוספה תמיכה בתוכן עברי, גרמני ואנגלי ובחיפוש רב־לשוני.
- מחיר גלובלי הוגדר במפורש כחיוב שאינו תלוי במספר השעות.
- הוחלט להשתמש בתצוגה מקומית במהלך הפיתוח במקום בפרסום Sites.
- השינויים עברו בנייה ובדיקה אוטומטית בהצלחה.
- התצוגה המקומית פעילה ב־`http://localhost:3000/`.
- נוספו מסכי לקוחות ועובדים עם רשימות ופעולות הוספה.
- נוסף מסך פרויקטים מלא עם חיפוש וסינון.
- נוסף טופס יצירת פרויקט עם בחירת לקוח, כתובת ושיוך עובדים.
- טופס הפרויקט תומך במחיר קבוע, חיוב לפי שעה ובשילוב, ומציג רק את שדות המחיר המתאימים לבחירה.
- הוספת לקוח, עובד או פרויקט מעדכנת מיד את הממשק המקומי.
- השלב עבר בנייה ובדיקה אוטומטית בהצלחה.
- מסכי הליבה נשמרו ב־Git ב־commit `3486291`.
- הוגדרו שני מצבי חשבון: עובד יחיד ומעסיק עובדים.
- נוסף מסך פרופיל לבחירת המצב, עם התאמה של הניווט, הסיכומים וטופס יצירת הפרויקט.
- מודל הנתונים הורחב לשמירת מצב העבודה ברמת החשבון.
- נוצרה מיגרציה `0001_account_modes.sql` לשמירת מצב החשבון במסד הנתונים.
- לקוחות, עובדים, פרויקטים ובחירת מצב החשבון מחוברים כעת ל־D1 המקומי ונשמרים לאחר רענון.
- ממשק השמירה מציג מצב שומר, נשמר או שגיאת שמירה.
- נתיב הנתונים המקומי נבדק בקריאה ובכתיבה, והמערכת הוחזרה למצב עובד.
- הבנייה והבדיקות האוטומטיות עברו בהצלחה לאחר חיבור הנתונים.
- שני מצבי החשבון וחיבור הנתונים נשמרו ב־Git ב־commit `a97297c`.
- נוספה עריכה מלאה של לקוחות, עובדים ופרויקטים, כולל תמחור ושיוך עובדים.
- נוספה מחיקה רכה ללקוחות, עובדים ופרויקטים; מחיקת לקוח מעבירה גם את הפרויקטים שלו לסל המחזור.
- פעולות העריכה והמחיקה זמינות במחשב ובטלפון ומחוברות למסד המקומי.
- מחזור יצירה, עריכה ומחיקה רכה נבדק בפועל עבור כל שלושת סוגי הרשומות.
- הבנייה והבדיקות האוטומטיות עברו בהצלחה.
- שלב העריכה והמחיקה הרכה נשמר ב־Git ב־commit `cdf70a5`.
- נתיבי הנתונים מחוברים לזהות המשתמש בצד השרת וכל חשבון מקבל מרחב נתונים נפרד.
- נוסף מסך התחברות למשתמש שאינו מזוהה וקישור התנתקות בפרופיל של משתמש מחובר.
- שם ואימייל המשתמש המחובר מוצגים בפרופיל ובממשק הראשי.
- סביבת הפיתוח המקומית ממשיכה לעבוד עם משתמש פיתוח קבוע וללא התחברות חיצונית.
- נוצרה מיגרציה `0002_account_identity.sql` המגדירה ייחודיות אימייל בתוך חשבון במקום ייחודיות גלובלית.
- הפרדת נתונים נבדקה בפועל: רשומה שנוצרה בחשבון בדיקה לא נחשפה למשתמש המקומי.
- הבנייה והבדיקות האוטומטיות עברו בהצלחה לאחר חיבור הזהות.
- חיבור הזהות והפרדת החשבונות נשמרו ב־Git ב־commit `32a58c1`.
- הטיימר מחובר למסד ושומר דיווח פתוח, זמן התחלה, זמן סיום ומשך מדויק.
- טיימר פעיל משוחזר לאחר רענון וממשיך להציג את הזמן שחלף.
- נוסף טופס דיווח שעות ידני עם פרויקט, תאריך, משך ותיאור רב־לשוני.
- נוספה רשימת דיווחי הזמן האחרונים בדף הראשי.
- שעות וסכומים בפרויקטים מחושבים מתוך דיווחי הזמן בהתאם למחיר גלובלי, שעתי או משולב.
- נוספו אינדקסים לשאילתות זמן לפי פרויקט ולפי טיימר פעיל במיגרציה `0003_time_tracking.sql`.
- הפעלה, עצירה ודיווח ידני נבדקו בחשבון בדיקה נפרד, כולל חישוב הסכום השעתי.
- שלב דיווחי הזמן וחישובי הפרויקטים נשמר ב־Git ב־commit `21e0c19`.
- נוסף מסך סל מחזור המציג לקוחות, פרויקטים ועובדים שנמחקו במחיקה רכה.
- ניתן לשחזר לקוח בלבד או יחד עם הפרויקטים המחוקים שלו; שחזור פרויקט מחזיר גם לקוח שנמחק, ושחזור עובד מחזיר אותו כפעיל.
- כל שאילתות סל המחזור ופעולות השחזור מוגבלות בצד השרת לעסק של המשתמש המחובר.
- מחזור API מלא של מחיקה ושחזור נבדק בחשבון בדיקה מבודד, והבנייה, בדיקת ה־SSR וה־lint עברו בהצלחה.
- שלב סל המחזור והשחזור נשמר ב־Git ב־commit `7153bd9`.
- נוסף מנגנון הזמנות עובדים עם קישור אישי חד־פעמי, תוקף של 14 יום וביטול קישור קודם בעת יצירת קישור חדש.
- קבלת הזמנה מחייבת חשבון מחובר עם אימייל תואם ומקשרת את הזהות לרשומת העובד הקיימת.
- מסך העובדים מציג מצב חיבור, הזמנה ממתינה וקישור שניתן להעתיק ולשלוח ידנית.
- עובד מחובר רואה רק פרויקטים ששויכו אליו, את דיווחי הזמן שלו ואת השכר לפי התעריף שלו; פעולות ומידע של מנהל נחסמים גם בצד השרת.
- נוצרה מיגרציה `0004_fat_whizzer.sql` עבור זהות מחוברת לעובד וטבלת ההזמנות.
- מחזור הזמנה מלא נבדק עם חשבונות מנהל ועובד נפרדים, כולל קבלת ההזמנה, הפרדת מידע, חסימת הרשאות וטיימר עובד.
- הבנייה, בדיקת ה־SSR וה־lint עברו בהצלחה.
- שלב הזמנות וחיבור העובדים נשמר ב־Git ב־commit `7323cea`.

## מתבצע כעת

- סיום ותיעוד של שלב הזמנות וחיבור עובדים.

## השלב הבא

1. להוסיף עריכה ומחיקה רכה של דיווחי זמן.
2. להגדיר בהמשך מדיניות מחיקה לצמיתות מסל המחזור.
3. לחבר שירות שליחת אימייל להזמנות כאשר תיבחר תשתית הדיוור.

## חסימות

- אין חסימה לפיתוח או לתצוגה המקומית. פרסום Sites אינו חלק מזרימת העבודה הנוכחית.

## הערה למפגש הבא

יש לקרוא את `docs/PRODUCT_PLAN.md`, אחריו את `docs/DECISIONS.md`, ולבסוף את הקובץ הזה. ממשיכים מהפריט הראשון תחת "השלב הבא" שלא הושלם.
